"""Independent verification harness.

This package is deliberately separate from ``src/adversarial_ai``. Nothing here
imports the repository's own evaluation or audit code: re-running the same
modules would only re-confirm the same assumptions. See
``docs/INDEPENDENT_VERIFICATION.md``.
"""
