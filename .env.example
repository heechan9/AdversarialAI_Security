# Copy this file to .env and fill in real values locally.
# NEVER commit the real .env file — it is excluded via .gitignore.
# Only the provider actually selected in docs/API_SELECTION.md needs a value.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
